# 自动拾取 (auto-pickup)

移植自 [BetterGI 自动拾取](https://www.bettergi.com/feats/timer/pick.html)：开启后每 250ms 发送一次 `F` 键（keydown+keyup，补齐 keyCode/code），路过掉落物/采集点自动交互。

- 适用于用 `F` 交互的 PC 类云游戏（原神、崩铁等）。
- BGI 的黑白名单需要 OCR 识别拾取项文字，云端无法移植，此版本为无差别按键。
